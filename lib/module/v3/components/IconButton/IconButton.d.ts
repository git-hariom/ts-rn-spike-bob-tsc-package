import React from 'react';
type IconButtonProps = {
    label: string;
    borderRadius?: number;
};
declare const IconButton: ({ label, borderRadius, }: IconButtonProps) => React.JSX.Element;
export default IconButton;
