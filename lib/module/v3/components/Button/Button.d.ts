import React from 'react';
type ButtonProps = {
    label: string;
    borderRadius?: number;
};
declare const Button: ({ label, borderRadius, }: ButtonProps) => React.JSX.Element;
export default Button;
